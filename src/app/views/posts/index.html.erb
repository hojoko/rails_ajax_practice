<div>　　</div>

<div id="content"><%= render 'content', posts: @posts %></div>

<%= form_with model: @post, local: false do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>  
  <%= f.submit "投稿" %>
<% end %>
  
